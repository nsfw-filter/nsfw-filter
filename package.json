{
  "name": "nsfw-blocker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "copy": "cp src/content.js dist/src/ && cp src/popup.html dist/src/",
    "build": "parcel build src/background.js -d dist/src/ -o background && npm run copy",
    "watch": "npm run copy && parcel watch src/background.js --hmr-hostname localhost -d dist/src/ -o background"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs": "^2.0.1",
    "nsfwjs": "^2.2.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
